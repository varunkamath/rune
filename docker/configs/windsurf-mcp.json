{
  "mcpServers": {
    "rune": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "${HOME}/Projects:/workspace:ro",
        "-v",
        "${HOME}/.rune:/data",
        "rune-mcp:latest",
        "node",
        "/app/dist/index.js"
      ]
    }
  }
}
