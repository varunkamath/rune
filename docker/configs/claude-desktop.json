{
  "mcpServers": {
    "rune": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "${HOME}:/workspace:ro",
        "-v",
        "${HOME}/.rune:/data",
        "rune-mcp:latest",
        "node",
        "/app/dist/index.js"
      ],
      "env": {}
    }
  }
}
