{
  "mcpServers": {
    "rune": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--entrypoint",
        "/usr/local/bin/start-mcp",
        "-v",
        "${cwd}:/workspace:ro",
        "-v",
        "${HOME}/.rune:/data",
        "-e",
        "RUNE_WORKSPACE_ID=${cwd}",
        "rune-mcp:latest"
      ],
      "env": {}
    }
  }
}
