{
  "mcpServers": {
    "rune": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--entrypoint",
        "/usr/local/bin/start-mcp",
        "-v",
        "${PWD}:/workspace:ro",
        "-v",
        "${HOME}/.rune:/data",
        "rune-mcp:latest"
      ],
      "env": {}
    }
  }
}
